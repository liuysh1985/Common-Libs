{{ ngx_log_path }}/*.log {
    create 0644 nginx nginx
    size 500M
    rotate 14
    missingok
    notifempty
    compress
    dateext
    dateformat -%Y-%m-%d
    sharedscripts
    postrotate
        /bin/kill -USR1 `cat /run/nginx.pid 2>/dev/null` 2>/dev/null || true
    endscript
}